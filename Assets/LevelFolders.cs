using UnityEngine;

public class LevelFolders : MonoBehaviour
{
    [SerializeField] private Transform listenerFolder;
    [SerializeField] private Transform triggerableFolder;
    [SerializeField] private Transform actorFolder;
    [SerializeField] private Transform obstacleFolder;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
